<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        .cards {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }
        .card {
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            max-width: 300px;
            width: 100%;
            transition: transform 0.3s;
        }
        .card:hover {
            transform: translateY(-10px);
        }
        .card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        .card-body {
            padding: 15px;
        }
        .card-title {
            font-size: 1.5em;
            margin: 0 0 10px;
            color: #333;
        }
        .card-content {
            font-size: 1em;
            color: #666;
            margin-bottom: 15px;
        }
        .card-footer {
            text-align: right;
            color: #999;
            font-size: 0.9em;
        }
        .read-more-btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #333;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            transition: background-color 0.3s;
        }
        .read-more-btn:hover {
            background-color: #575757;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>
    <div class="container">
        <h1>Welcome to BlogDotCom</h1>
        <div class="cards">
            <% blogs.forEach(function(blog) { %>
                <div class="card">
                    <img src="<%= blog.coverImageURL %>" alt="Blog Cover">
                    <div class="card-body">
                        <h2 class="card-title"><%= blog.title %></h2>
                        <p class="card-content"><%= blog.content.substring(0, 100) %>...</p>
                        <a href="/blog/<%= blog._id %>" class="read-more-btn">Read More</a>
                        <div class="card-footer">
                            <small>Posted on <%= new Date(blog.createdAt).toLocaleDateString() %></small>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>
