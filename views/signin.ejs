<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogDotCom - Signin</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .navbar {
            background-color: #333;
            width: 100%;
            padding: 14px 0;
            color: white;
            text-align: center;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            padding: 0 15px;
            font-size: 18px;
        }
        .navbar a:hover {
            background-color: #555;
        }
        .form-container {
            background: white;
            padding: 2em;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            margin-top: 2em; /* Add margin to separate from navbar */
        }
        h1 {
            text-align: center;
            color: #333;
        }
        .form-group {
            margin-bottom: 1em;
        }
        label {
            display: block;
            margin-bottom: 0.5em;
            color: #666;
        }
        input {
            width: 100%;
            padding: 0.75em;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1em;
        }
        input:focus {
            border-color: #333;
        }
        .form-group:last-child {
            text-align: center;
        }
        button {
            background-color: #007BFF;
            color: white;
            padding: 0.75em 2em;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }
        button:hover {
            background-color: #0056b3;
        }
        .form-footer {
            text-align: center;
            margin-top: 1em;
            color: #666;
        }
        .form-footer a {
            color: #007BFF;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %> <!-- Include navbar -->
    <div class="form-container">
        <h1>Sign In</h1>
        <form action="/user/signin" method="post">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" placeholder="Enter your fancy email">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" placeholder="Shh... it's a secret">
            </div>
            <div class="form-group">
                <button type="submit">Let Me In</button>
            </div>
        </form>
        <div class="form-footer">
            New here? <a href="/user/signup">Sign Up</a>
        </div>
    </div>
</body>
</html>
